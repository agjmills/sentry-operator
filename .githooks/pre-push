#!/usr/bin/env bash
set -euo pipefail

echo "==> go test"
go test -race ./...

echo "==> helm lint"
helm lint charts/sentry-operator/

echo "==> goreleaser check"
if command -v goreleaser &>/dev/null; then
  goreleaser check
else
  echo "    goreleaser not found, skipping (CI will catch this)"
fi
